## Person Records

* Person records — structured records with name, role, bio, photo, and contact details
* Department grouping — assign persons to one or more departments / teams
* FAL images — portrait photos managed via TYPO3 File Abstraction Layer
* vCard export — optional vCard download per person

## Frontend Display

* Frontend listing — filterable team overview grid
* Detail view — individual profile page per person
